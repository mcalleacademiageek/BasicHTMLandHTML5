CSS Flexbox: Use the justify-content Property in the Tweet Embed

The last challenge showed an example of the justify-content property. For the tweet embed, this property can be applied to align the items in the .profile-name element.

Add the CSS property justify-content to the header's .profile-name element and set the value to any of the options from the last challenge.


CSS Flexbox: use la propiedad justify-content en el Tweet Embed

El último desafío mostró un ejemplo de la propiedad justify-content. Para la inserción de tweets, esta propiedad se puede aplicar para alinear los elementos en el elemento .profile-name.

Agregue la propiedad CSS justify-content al elemento .profile-name del encabezado y establezca el valor en cualquiera de las opciones del último desafío.



